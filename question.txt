1.hash冲突解决办法：拉链法、线性探测发、二次hash
2.hashMap的扩容原理，初始有13个，要怎么new？(达到了负载因子，直接手动>>1)
初始默认 capacity是16啊，面试官是问我假如初始有13个元素要怎么做，如果直接new，达到了0.75的负载因子会自动扩容，损耗性能，不如直接new HashNap(32)
3.Integer的常量缓存池的问题(-127~128范围有个cache)
4.ConcurrentHashMap的size()怎么做的，仔细看下
5.Spring的AOP关于拦截private方法一些问题

1.锁的机制和事务注解，如何保证了事务的一致性
2.spring层面做事务和数据库层面做的区别，各自实现方式
3.事务的传播性和隔离级别，问了mysql的默认隔离级别（可重复读）
4.spring中事务传播性怎么配置（xml方式和注解方式，还有关于savepoint的使用）
5.O(1)删除执行链表结点，做分析（其实是要指出剑指offer中那个直接copy值的方法的缺陷）
6.二叉树的最长距离（递归的思想）

1.关于AOP在spring的应用（比如事务，通知，aspectJ）
2.jdk代理和cglib第三方代理（说出对接口代理和子类继承的区别）
3.前缀索引
4.char，varchar，text，blob的关系和区别
5.为什么索引不能随便用，什么时候用（什么时候失效，什么时候最高效）
6.如何达到索引开销和性能的平衡（方法就是，根据情境看经常做的查询是哪些，然后依次是什么查询条件，保证最高效索引的同时，也保证索引不失效，避免无效开销，并且根据show profile和explain功能进行对比分析）
7.redis的理解（用作缓存，队列，也可做存储）

1.网络时延如何处理
2.数据库左连接右连接，场景
3.git 常用的操作，git rebase和git merge区别
4.分布式数据库怎么调用
5.linux常用命令，查看内存，查看磁盘使用率

1.tomcat类加载
2.悲观锁乐观锁，底层怎么实现的，越详细越好
3.如何防止内存泄露，哪些会容易造成
4.jvm调优如何检查内存泄露，如何优化gc参数
5.旋转数组中查找某一个值

1.http请求和返回头
2.海量数据找到出现次数最多的100个（内存不足的时候可以先做hash分片，最后多路merge，每次操作可以用hashMap计数，也可以自己做hash函数计数）
3.jvm内存模型，分代，cpu100% 怎么排查（我以为问Jconsole的使用，其实是想问linux性能监测和调优）
4.nio selector中的wakeup什么含义
5.select poll epoll （linux内核相关的知识）
6.arraylist.sort怎么实现的（这个可以看看TimSort的思想）

1.100亿个数找最大1000个（说了分片，用堆，再归并）
2.一个进程最多申请多大空间（看机器cpu的处理位数看情况）
3.TCP 3次握手和timewait讲一下原理
4.TCP的滑动窗口是什么，饱和了怎么处理
5.get和post请求
6.linux怎么查看网络状态（vmstat）

1.hibernate和mybatis各自优缺点
2.mybatis和hibernate各自的缓存原理和比较，hibernate的一级二级和查询缓存
3.synchronized关键字，实现原理（和Lock对比着说，说到各自的优缺点，synchronized从最初性能差到jdk高版本后的锁膨胀机制，大大提高性能，再说底层实现，Lock的乐观锁机制，通过AQS队列同步器，调用了unsafe的CAS操作，CAS函数的参数及意义；同时可以说说synchronized底层原理，jvm层的moniter监视器，对于方法级和代码块级，互斥原理的不同，+1-1可重入的原理等）
4.ArrayList扩容机制以及扩容时的copy过程

1.消息队列，讲了一下几种模型和原理
2.讲讲Spring中怎么对初始化的bean做其他操作。（这里有三种方式，@PostConstruct注解方式，init-method的XML配置方式，InitializingBean接口方式）
3.三种实现上的区别（还好看过点源码，其实前两种是一个意思，都是通过反射的方式用aop思想实现，可以消除对spring的依赖；接口方式是直接调用afterPropertiesSet方法，效率更高点。spring加载bean时先判断接口方式，再执行配置注解方式）

1.数据库主从备和读写分离原理
2.最长递增子序列，一个dp数组一个max数组，最优情况

1.ConcurrentHashMap jdk7和jdk8的区别，Collections.sort函数jdk7 和 jdk8 分别怎么实现的。
2.CopyOnWriteList底层是什么，适用的情况，vector的特点，实现的是List接口吗
3.线程间通信三种方式
4.锁的膨胀过程，Synchronized和Lock的区别，底层的monitor实现和unsafe类的CAS函数，参数表示什么，寄存器cpu如何做）
5.volatile cpu和寄存器层面是怎么实现的。
6.各种类型线程池各自的特点，应用场景。
7.为什么匿名内部类的变量必须用final修饰，编译器为什么要这么做，否则会出现什么问题
8.索引的分类
9.主键索引和普通索引的区别，组合索引怎么用会失效
10.索引的前缀匹配的原理，从B树的结构上具体分析一下
11.聚集索引在底层怎么实现的，数据和关键字是怎么存的
12.组合索引和唯一性索引在底层实现上的区别
13.sql的优化策略，慢查询日志怎么操作，参数含义
14.explain 每个列代表什么含义
15.show profile 怎么使用

1.get post区别，post可不可以用url的方式传参
2.说到了url有最大长度，就问长度有限制是get的原因还是url的原因，为什么长度会有限制，是http数据包的头的字段原因还是内容字段的原因，详细说明
3.关于幂等性的详细介绍
4.幂等性是http层面的问题吗，还是服务器要处理和解决的内容。（就是看你对幂等性的定性是怎么理解的）
5.后台服务器对于一个请求是如何做负载均衡的，有哪些策略，会出现什么样的问题，怎么解决
6.说说http的缺点，无状态，明文传输
7.https ssl tcp三者关系，其中哪些用到了对称加密，哪些用到了非对称加密，非对称加密密钥是如何实现的
8.关于加密的私钥和公钥各自如何分配（客户端拿公钥，服务器拿私钥）
9.那客户端是如何认证服务器的真实身份，详细说明一下过程，包括公钥如何申请，哪一层加密哪一层解密。







